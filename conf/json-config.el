;;;========================================
;;; JavaScript系の設定
;;;========================================

;; 拡張子jsonのファイルを開いたときjson-modeに
(add-to-list 'auto-mode-alist '("\\.json$" . json-mode))

(provide 'json-config)
